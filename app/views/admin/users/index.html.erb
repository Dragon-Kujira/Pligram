<div class="container">
  <h4 class="m-4"><span style="background-color:#eeeeee">ユーザー一覧</span></h4>
</div>

 <div class="container">
  <div class="row">
   <table class="table">
    <thead class=" thead-light ">
      <tr>
        <th>会員ID</th>
        <th>氏名</th>
        <th>メールアドレス</th>
        <th>ステータス</th>
      </tr>
    </thead>
    <tbody>
      <% @user.each do |user| %>
        <tr>
          <td><%= user.id %></td>
          <td><%= link_to user.name,admin_user_path(user.id) %></td>
          <td><%= user.email %></td>

          <% if user.is_active == true %>
          <td class=" text-success ">有効</td>
          <% else %>
        　<td class=" text-secondary ">退会</p>
          <% end %>
        </tr>
        <% end %>
      </tbody>
    </table>
 
  </div>
</div>
