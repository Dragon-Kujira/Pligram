<% if post.favorited_by?(current_user) %>
<p>
  <%= link_to post_favorite_path(post), method: :delete, remote: true do %>
    ★<%= post.favorites.count %> ブクマ
  <% end %>
</p>
<% else %>
<p>
  <%= link_to post_favorite_path(post), method: :post, remote: true do %>
    ☆<%= post.favorites.count %> ブクマ
  <% end %>
</p>
<% end %>